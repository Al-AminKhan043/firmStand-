<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>
<body>
     <h1><%= category %> Products!</h1>
    <ul>
<% for (let product  of products) { %>
       <li> <a href="/products/<%=product._id%>"> <%=product.name%>   </li>
   
 <% } %>
</ul>

<a href="/products/new">Add New Product</a> <br>
<% if (category !== 'All') { %>
<a href="/products">All Products</a>
<% } %>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light grey background */
        }

        .card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
             
        }

        .card:hover {
            transform: scale(1.05); /* Slight zoom effect */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2); 
            background-color: #7ed3df;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #343a40; /* Dark grey */
        }

        a {
            text-decoration: none;
        }

        .btn-custom {
            background-color: #007bff; /* Bootstrap primary blue */
            color: white;
        }

        .btn-custom:hover {
            background-color: #0056b3;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="container py-5">
        <h1><%= category %> Products!</h1>

        <div class="row g-4">
            <% for (let product of products) { %>
                <div class="col-md-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="card-text">Price: $<%= product.price %></p>
                            <p class="card-text">Category: <%= product.category %></p>
                            <a href="/products/<%= product._id %>" class="btn btn-custom">View Details</a>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>

        <div class="mt-5 text-center">
            <a href="/products/new" class="btn btn-success">Add New Product</a> <br>
            <% if (category !== 'All') { %>
                <a href="/products" class="btn btn-secondary mt-2">All Products</a>
            <% } %>
        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
